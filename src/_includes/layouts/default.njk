<!-- index.njk -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }}</title>
</head>
<body>
  <h1>Welcome to My Website</h1>

  {% for item in collections.devarsh %}
    <h2>{{ item.title }}</h2>
    <div>{{ item.content | safe }}</div>  <!-- Render the HTML content -->
  {% endfor %}
</body>
</html>
